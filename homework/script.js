function addToBasket(quantity, price) {
    let totalPrice = quantity * price;
    alert(`${quantity} Persian Rug for ${totalPrice}€ added to your basket`);
    console.log(`${quantity} Persian Rug for ${totalPrice}€ added to your basket`);
}
